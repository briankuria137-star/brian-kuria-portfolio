# Branding assets

Add these files here once ready:

- `og-image.png` — 1200x630 Open Graph / social share preview (referenced in `app/layout.tsx`)
- Favicon files (referenced in `app/layout.tsx` metadata `icons`)
