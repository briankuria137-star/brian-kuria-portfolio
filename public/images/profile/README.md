Add the profile photograph here, then update the Hero and About sections
(`components/sections/Hero.tsx`, `components/sections/About.tsx`) to render
it with `next/image` instead of the placeholder frame.
